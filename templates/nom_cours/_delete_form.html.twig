<form method="post" action="{{ path('app_nom_cours_delete', {'id': nom_cour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nom_cour.id) }}">
    <button class="btn">Delete</button>
</form>
