{% extends 'base.html.twig' %}


{% block body %}
    <h1>Détails du nom de cours</h1>

    <p>Nom : {{ nom_cour.nom }}</p>

    <a href="{{ path('app_nom_cours_edit', {'id': nom_cour.id}) }}">Modifier</a>
    <a href="{{ path('app_nom_cours_index') }}">Retour à la liste</a>
{% endblock %}

{% block title %}NomCours{% endblock %}

{% block body %}
    <h1>NomCours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nom_cour.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ nom_cour.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nom_cours_index') }}">back to list</a>

    <a href="{{ path('app_nom_cours_edit', {'id': nom_cour.id}) }}">edit</a>

    {{ include('nom_cours/_delete_form.html.twig') }}
{% endblock %}

