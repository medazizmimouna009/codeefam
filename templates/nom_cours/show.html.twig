{% extends 'base.html.twig' %}

{% block body %}
    <h1>Détails du nom de cours</h1>

    <p>Nom : {{ nom_cour.nom }}</p>

    <a href="{{ path('app_nom_cours_edit', {'id': nom_cour.id}) }}">Modifier</a>
    <a href="{{ path('app_nom_cours_index') }}">Retour à la liste</a>
{% endblock %}