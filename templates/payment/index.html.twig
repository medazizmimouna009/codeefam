<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stripe Payment</title>
    <script src="https://js.stripe.com/v3/"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }
        .payment-container {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        h2 {
            color: #007bff;
        }
    </style>
</head>
<body>

    <div class="payment-container">
        <h2>Stripe Payment</h2>
        
        <form id="payment-form" action="{{ path('create_payment_session') }}" method="POST">
            <input type="hidden" name="user_id" value="{{ app.user.id }}">
            <input type="hidden" name="offre_id" value="{{ offre.id }}">
            <input type="hidden" name="offre_name" value="{{ offre.type }}">
            <input type="hidden" name="offre_price" value="{{ offre.prix }}">
            <button type="submit" id="checkout-button" class="btn btn-primary mt-3">Pay Now</button>
        </form>
    </div>

</body>
</html>