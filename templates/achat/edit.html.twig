{% extends 'base.html.twig' %}

{% block title %}Edit Achat{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4 text-center text-warning">Modifier l'Achat</h1>

        <div class="card p-4 shadow-lg border-0 rounded-3 bg-light">
            {{ include('achat/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div class="text-center mt-4">
            <a href="{{ path('app_achat_index') }}" class="btn btn-outline-secondary btn-lg">⬅ Back to List</a>
        </div>

        <div class="mt-3 text-center">
            {{ include('achat/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}