<form method="post" action="{{ path('app_achat_delete', {'idAchat': achat.idAchat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ achat.idAchat) }}">
    <button class="btn">Delete</button>
</form>
